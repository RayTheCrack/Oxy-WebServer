// Minimal WOW.js shim to avoid runtime errors and preserve simple animation API
(function(window){
	function WOWShim() {}
	WOWShim.prototype.init = function() { /* no-op for shim */ };
	// expose globally
	window.WOW = WOWShim;
})(window);
